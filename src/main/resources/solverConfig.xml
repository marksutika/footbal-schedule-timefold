<?xml version="1.0" encoding="UTF-8"?>
<solver>
    <solutionClass>lv.football.scheduler.domain.SchedulingSolution</solutionClass>
    <entityClass>lv.football.scheduler.domain.Match</entityClass>

    <scoreDirectorFactory>
        <constraintProviderClass>
            lv.football.scheduler.constraint.ScheduleConstraintProvider
        </constraintProviderClass>
    </scoreDirectorFactory>

    <termination>
        <secondsSpentLimit>10</secondsSpentLimit>
    </termination>
</solver>